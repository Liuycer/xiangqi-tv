# Phase 33 设置界面精简

## 调整内容

- “设置与棋谱”的设置区只保留“落子音效”和“走子动画”两个功能开关。
- 删除设置区的“重置水平”；该操作只保留在棋手档案详情中。
- “返回对局”按钮删除右侧“关闭”状态文字，只显示操作名称。
- 移除设置区中与自适应评级相关的错误占位，避免混入非设置内容。
- 遥控器焦点范围调整为 0–2：音效、动画、返回对局；OK 不会触发已移除的重置入口。

## 验证

- 前端 14 个测试文件、91 项测试通过。
- TypeScript 检查与 Vite 生产构建通过。
- 使用 VPS 当前 Token 构建，APK 内 Token 一致性校验通过。
- APK 4,053,280 bytes，SHA-256 `26d6da36b0d4c551000a14874821588074f6a850c4c0b1b40d51436611c0e710`。
- 已覆盖安装至 MagicBox_M_30_F，冷启动 3489ms，主 Activity 正常前台运行。
- 真机打开设置面板验收：仅显示音效、动画、返回三个控件；无重置入口，“返回对局”右侧无“关闭”文字，界面未溢出。
